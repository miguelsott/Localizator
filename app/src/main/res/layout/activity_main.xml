<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#536DFE"
    tools:context="com.example.android.localizator.main.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"> <!-- Adicionado centralização às view filhas -->

        <!-- Adicionado imagem com especificações dadas -->
        <ImageView
            android:layout_width="300dp"
            android:layout_height="300dp"
            android:layout_marginBottom="8dp"
            android:layout_marginTop="24dp"
            android:src="@drawable/logo" />

        <!-- Adicionado shape como background dos botões,
             Margem de 16dp, cor da fonte branca -->
        <Button
            android:id="@+id/btn_add_address"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:background="@drawable/botao_retangular"
            android:text="@string/add_address"
            android:textColor="@android:color/white" />

        <Button
            android:id="@+id/btn_show_addresses"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:background="@drawable/botao_retangular"
            android:text="@string/show_addresses"
            android:textColor="@android:color/white" />

    </LinearLayout>

</ScrollView>
